[package]
name = "bootloader"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
uefi.workspace = true

boot-protocol.workspace = true
system.workspace = true
config.workspace = true

x64.workspace = true
acpi.workspace = true
elf.workspace = true
utils.workspace = true

log.workspace = true
log-debugcon.workspace = true

spinlocks.workspace = true

pci = { path = "../../dev/pci", default-features = false, features = [] }

[build-dependencies]
builder.workspace = true

[features]
default = ["log-debugcon"]
log-debugcon = []
